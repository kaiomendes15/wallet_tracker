# Nome da Aplicação
spring.application.name=mywallet
api.security.token.secret=${JWT_SECRET:fortnite2018}

# Define o perfil ativo por padrão.
# Isso diz ao Spring: "Por favor, carregue também o arquivo application-dev.properties"
spring.profiles.active=dev